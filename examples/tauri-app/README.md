# Vnidrop Share Example

This app exercises `@vnidrop/tauri-plugin-share` from a Tauri 2 frontend.

```sh
npm install
npm run tauri dev
```

The example includes `vnidrop-share:allow-cleanup` because cleanup is intentionally not part of the plugin's default permission set.
